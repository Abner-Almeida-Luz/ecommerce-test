spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ecommerce_test_test?sslmode=disable
    username: postgres
    password: postgres123

  flyway:
    enabled: true
    locations: classpath:db/migration
    clean-on-validation-error: true

  jpa:
    hibernate:
      ddl-auto: validate